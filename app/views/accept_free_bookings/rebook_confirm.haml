- content_for :title_header do
  %h1= t("layouts.no_tribe.inbox")

.conversation-status-form.centered-section
  %h2= t('free_bookings.rebook_confirm.heading')
  %p=  t('free_bookings.rebook_confirm.body')
  = form_for @listing_conversation, :url => rebook_free_booking_person_message_path(:person_id => @current_user.id, :id => @listing_conversation.id), :html => { :method => "put" } do |form|
    = form.button t('free_bookings.rebook_confirm.submit'), :class => "send_button"
